apiVersion: v1
kind: Service
metadata:
  name: {{ include "riskmonitor-frontend.fullname" . }}
  labels:
    {{- include "riskmonitor-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "riskmonitor-frontend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
